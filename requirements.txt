# Core runtime dependencies for the NTT Pricing Tool
ezdxf>=1.1        # AutoCAD DXF single line diagram parsing
openpyxl>=3.1     # Excel pricing sheets / load schedules
rapidfuzz>=3.0    # fuzzy component matching
requests>=2.28    # EPLAN Data Portal API client
Jinja2>=3.1       # quotation HTML rendering
Flask>=3.0        # local web upload interface (python -m ntt_pricing.web)
pymupdf>=1.23     # read single line diagrams supplied as PDF
